import core.SimulationManager;

public class Main {
    public static void main(String[] args) {
        int maxCapacity = 10; // Example max capacity
        SimulationManager manager = new SimulationManager(maxCapacity);
        manager.startSimulation();
    }
}
